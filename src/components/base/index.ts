/**
 * Base Components
 *
 * Primitive UI building blocks - the foundation of any interface.
 * These are highly reusable, atomic components with minimal dependencies.
 *
 * Categories:
 * - Buttons: Featured icons, buttons, social buttons, app store buttons, utility buttons, button groups
 * - Indicators & Feedback: Badges, badge groups, tags
 * - Form Controls: Dropdowns, select, inputs, textareas, verification inputs, text editors,
 *                  toggles, checkboxes, radio buttons, radio groups
 * - Display Elements: Avatars, tooltips, progress indicators, sliders, video players,
 *                     credit cards, QR codes, illustrations, rating badges/stars
 *
 * Total: 28 component types, 200+ variants
 *
 * @see https://www.untitledui.com/react/components (Base Components section)
 */

// Export base components as they are created
// Example: export * from './buttons';
// Example: export * from './avatar';

// Empty export to make this a valid module
export {};
